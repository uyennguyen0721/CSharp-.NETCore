@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@*
    File này tương tự như ViewStart, cũng chèn tự động vào các file cshtml trong cùng thư mục, tuy nhiên trong file này chỉ chấp nhận đưa vào đây một
    số chỉ thị ví dụ như: @addTagHelper, @using (nạp namespace), @inject (để inject các dependence), ...
*@